{
  "name": "ai-workspace-assistant-mvp",
  "version": "1.0.0",
  "description": "Web AI workspace with streaming chat and session persistence",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.1"
  }
}
